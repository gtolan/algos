//Pivot and partition
//bigger values to one side of the pivot smaller to the other
//recursive breaking list smaller 

const partition = (arr,left,right) => {


}

const quickSortRecursive = (arr,start, end)=> {



}

const quickSort = (arr) => {
    quickSortRecursive(arr, start =0, end=arr.length-1)
}

const arr = [1,4,53,45,2,4,9,14,23]